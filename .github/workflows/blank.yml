name: Run test.bat locally

on:
  workflow_dispatch:  # allows manual run from GitHub Actions tab

jobs:
  run-local:
    runs-on: self-hosted  # 👈 ensures it runs on your Windows runner
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          path: "C:\\Users\\Yunus Dermawan\\Documents\\testgit"

      - name: Execute test.bat
        run: |
          echo "Running test.bat..."
          cd "C:\\Users\\Yunus Dermawan\\Documents\\testgit"
          .\test.bat
        shell: pwsh
